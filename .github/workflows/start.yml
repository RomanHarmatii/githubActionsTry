name: Workflow name
run-name: Workflow name run name
on: workflow_dispatch
jobs:
  firstJob:
    name: "first job"
    runs-on: ubuntu-latest
    steps:
      - name: Print a greeting
        run: echo "hello world!"
